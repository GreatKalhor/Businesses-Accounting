@model IEnumerable<Businesses_Accounting.Models.Contact>

@{
    ViewData["Title"] = "اشخاص";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<p>
    <a asp-action="Create">شخص جدید</a>
</p>
@(
    Html.Kendo().Grid<Businesses_Accounting.Models.Contact>()
                           .Name("grid")
                           .Columns(columns =>
                           {
                               columns.Bound(model => model.DisplayName);
                               columns.Bound(model => model.Company);
                               columns.Bound(model => model.IsActive);
                               columns.Bound(model => model.Id).Filterable(false).ClientHeaderTemplate(" ")
                               .ClientTemplate(@"<a href='" + @Url.Action("Edit", "Contacts", new { Id = "#:data.Id#" }) + @"'>Edit</a> |  <a href='" + @Url.Action("Delete", "Contacts", new { Id = "#:data.Id#" }) + @"'>Delete</a>  ").Width(430);
                })

                           .Pageable()
                           .Sortable()
                           .Scrollable()
                           //.Groupable()
                           .Filterable()
                           .HtmlAttributes(new { style = "height:550px;" })
                           .DataSource(dataSource => dataSource
                               .Ajax()
                               .PageSize(20)
                               .Read("List", "Contacts")
                            )
       )
